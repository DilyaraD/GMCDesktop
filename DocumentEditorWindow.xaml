<Window x:Class="GeotekMetallCompleteDesktop.DocumentEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Редактор документов" Height="800" Width="1000"
        WindowStartupLocation="CenterScreen" MinHeight="600" MinWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ToolBar Grid.Row="0">
            <Button Content="Вставить изображение" Click="InsertImage_Click" 
                    ToolTip="Вставить изображение в документ" Padding="10,3"/>
        </ToolBar>

        <RichTextBox x:Name="DocumentEditor" Grid.Row="1" Margin="10" 
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <RichTextBox.Resources>
                <Style TargetType="{x:Type Paragraph}">
                    <Setter Property="Margin" Value="1"/>
                </Style>
            </RichTextBox.Resources>
        </RichTextBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="Отмена" Click="Cancel_Click" Width="100" Margin="5"  Style="{StaticResource CancelButtonStyle}"/>
            <Button Content="Сохранить" Click="Save_Click" Width="100" Margin="5" Style="{StaticResource PrimaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>